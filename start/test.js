const user = {
    name:'karan',
    taskq:'coder'
}

function handleObjects(anyobjects){
    console.log(`username is ${anyobjects.name} and task is ${anyobjects.task}`)

}


handleObjects(user)

myar = [1,2,3,4,]

function seconsa(arr){
    console.log(arr[1])
}
seconsa(myar)
